myoutlier=function(vect){
	q1=quantile(vect,0.25)
	q3=quantile(vect,0.75)
	iq=IQR(vect)
	q1<-q1-(1.5*iq)
	q3<-q3+(1.5*iq)
	ans=c(1)
	k=1
	for(i in vect){
		if(i>q3){
			ans[k]<-i
			k<-k+1
		}
		if(i<q1){
			ans[k]<-i
			k<-k+1
		}
	}
	return(ans)
}
